@model List<Product>
@* 
    \@ işareti Razor Operetor olarak adlandırılır
    Razor Operator: C# kodu yazacağımızı belirtmek için kullanılır
*@

 <h1>
    Product Main Page
 </h1>

@* @foreach (Product p in Model)
{
        <div>
            <h3 class="text-danger">@p.Name</h3>
            <p>@p.Price</p>
        </div>
} *@

<table class="table table-sm table-bordered table-hover">
    <tr class="bg-light">
        <th>Id</th>
        <th>Name</th>
        <th>Price</th>
    </tr>
    @for (int i = 0; i < Model.Count; i++)
    {
            <tr>
                <td>@Model[i].Id</td>
                <td>@Model[i].Name</td>
                <td>@Model[i].Price</td>
            </tr>
    }
</table>
